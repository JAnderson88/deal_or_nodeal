// const scores = [2,4,2,6,1,7,8,9,2,1];

// function kinderCandy(scores){
//     // console.log(scores);
//     const candies = [];
//     scores.forEach((student, index) =>{
//         console.log(`Student at seat ${index}: 
//             Has student ${index - 1} and ${index + 1} next to him,
//             Student before him has a score of ${(index-1 < 0) ? 'none because there is no one' : scores[index-1] }, ${(student <= scores[index-1]) ? :}
//             Student after him has a score of ${(index+1 >= scores.length) ? 'none because there is no one' : scores[index+1] }

//             ` 
            
//         );
//     });
// }

// kinderCandy(scores);